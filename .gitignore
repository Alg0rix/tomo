# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
.env

# uv / build
dist/
build/
*.egg-info/

# OS / editor
.DS_Store
.idea/
*.swp

# Tomo runtime data (stub)
app/data/*.db
app/data/*.json
!app/data/seed.json

# Local scratch / reference trees (not shipped)
tmp/

# Harbor / Terminal-Bench job outputs
benchmarks/terminal_bench/jobs/

# Runtime state (created at install / first run)
var/
shared/
logs/
artifacts/
# Local agent scratch at repo root (not defaults/agents/)
/agents/
# Runtime state dir at repo root only — do not ignore connector/internal/state/
/state/

# Built connector binary
connector/tomo-connector
.worktrees/
