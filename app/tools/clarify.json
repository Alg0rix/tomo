{
  "id": "clarify",
  "name": "Clarify",
  "description": "Ask the user a clarifying question before continuing.",
  "backend": "app.runtime.tools.clarify",
  "schema": {
    "type": "function",
    "function": {
      "name": "clarify",
      "description": "Return CLARIFY: {question} for the channel to surface to the user.",
      "parameters": {
        "type": "object",
        "properties": {
          "question": {
            "type": "string",
            "description": "Question to ask the user."
          }
        },
        "required": ["question"]
      }
    }
  }
}
