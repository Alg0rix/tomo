{
  "id": "calculator",
  "name": "Calculator",
  "description": "Evaluate basic arithmetic expressions.",
  "backend": "app.runtime.tools.calculator",
  "schema": {
    "type": "function",
    "function": {
      "name": "calculator",
      "description": "Evaluate a math expression.",
      "parameters": {
        "type": "object",
        "properties": {
          "expression": {
            "type": "string",
            "description": "Arithmetic expression to evaluate (numbers and + - * / // % ** with parentheses)."
          }
        },
        "required": ["expression"]
      }
    }
  }
}
