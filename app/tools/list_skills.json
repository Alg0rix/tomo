{
  "id": "list_skills",
  "name": "List Skills",
  "description": "List skills registered in the platform catalog.",
  "backend": "app.runtime.tools.skills_tools",
  "schema": {
    "type": "function",
    "function": {
      "name": "list_skills",
      "description": "Return a compact paginated skill catalog. Descriptions are shortened for discovery; use_skill loads the full body.",
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "Optional case-insensitive filter across skill id, name, and description."
          },
          "offset": {
            "type": "integer",
            "description": "Zero-based skill offset for the next catalog page (default 0)."
          },
          "limit": {
            "type": "integer",
            "description": "Maximum skills per page (default 24, max 100)."
          }
        },
        "required": []
      }
    }
  }
}
