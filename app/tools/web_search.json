{
  "id": "web_search",
  "name": "Web Search",
  "description": "Search the web via DuckDuckGo (no API key).",
  "backend": "app.runtime.tools.web_search",
  "schema": {
    "type": "function",
    "function": {
      "name": "web_search",
      "description": "Return top web search result titles, snippets, and URLs for a query.",
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "Search query."
          }
        },
        "required": ["query"]
      }
    }
  }
}
