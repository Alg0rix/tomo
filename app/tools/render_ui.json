{
  "id": "render_ui",
  "name": "Render UI",
  "description": "Render a safe, interactive declarative UI in the chat. Use for dashboards, forms, tables, charts, diagrams, and structured interactive results. For a full custom app with arbitrary HTML/JS, save an HTML artifact instead.",
  "backend": "app.runtime.tools.render_ui",
  "schema": {
    "type": "function",
    "function": {
      "name": "render_ui",
      "description": "Render or update an interactive UI tree in the chat. The tree is declarative JSON, not HTML or JavaScript. Supported nodes: text, markdown, card, stack, grid, badge, divider, table, chart, mermaid, image, link, input, select, button. Use stable ui_id values. mode=replace sends a full tree; mode=patch sends JSON patch operations such as {op:replace,path:/tree/children/0/value,value:...} and optional state. Every input/select/button needs an id and action; actions are sent through the typed UI action bridge. Use save_artifact for arbitrary HTML/JS mini-apps.",
      "parameters": {
        "type": "object",
        "properties": {
          "ui_id": {"type": "string", "description": "Stable UI instance id (letters, numbers, _, ., :, -)."},
          "mode": {"type": "string", "enum": ["replace", "patch"], "description": "replace renders a full tree; patch applies up to 50 JSON operations to the existing ui_id."},
          "tree": {"type": "object", "description": "Root declarative UI node. Containers use children; table uses columns/rows; chart uses data [{label,value}]. Required for replace."},
          "patch": {"type": "array", "description": "Patch operations: {op:add|replace|remove,path:/tree/... or /state/...,value:...}. Required when patch mode has no tree."},
          "state": {"type": "object", "description": "Serializable UI state to retain across updates and reloads."}
        },
        "required": ["ui_id"]
      }
    }
  }
}
