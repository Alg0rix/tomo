{
  "id": "web_fetch",
  "name": "Web Fetch",
  "description": "Fetch a public HTTP(S) URL and return readable Markdown (HTML pages) or plain text.",
  "backend": "app.runtime.tools.web_fetch",
  "schema": {
    "type": "function",
    "function": {
      "name": "web_fetch",
      "description": "HTTP GET a URL. HTML responses are converted to Markdown. Private/loopback hosts are blocked.",
      "parameters": {
        "type": "object",
        "properties": {
          "url": {
            "type": "string",
            "description": "http or https URL to fetch."
          }
        },
        "required": ["url"]
      }
    }
  }
}
