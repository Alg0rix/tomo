{
  "id": "browser_forward",
  "name": "Browser Forward",
  "description": "Go forward in the tab history.",
  "backend": "app.runtime.tools.browser_tools",
  "executor": "client",
  "target": "browser",
  "schema": {
    "type": "function",
    "function": {
      "name": "browser_forward",
      "description": "History forward in an authorized tab.",
      "parameters": {
        "type": "object",
        "properties": {
          "tab_id": {
            "type": "string"
          }
        },
        "required": [
          "tab_id"
        ]
      }
    }
  }
}
