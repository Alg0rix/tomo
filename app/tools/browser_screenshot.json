{
  "id": "browser_screenshot",
  "name": "Browser Screenshot",
  "description": "Capture a screenshot of an authorized tab.",
  "backend": "app.runtime.tools.browser_tools",
  "executor": "client",
  "target": "browser",
  "schema": {
    "type": "function",
    "function": {
      "name": "browser_screenshot",
      "description": "Screenshot an authorized tab (PNG). Prefer browser_snapshot for structured interaction.",
      "parameters": {
        "type": "object",
        "properties": {
          "tab_id": {
            "type": "string"
          }
        },
        "required": [
          "tab_id"
        ]
      }
    }
  }
}
