{
  "id": "recall",
  "name": "Recall",
  "description": "Search the knowledge base for facts matching a query.",
  "backend": "app.runtime.tools.recall",
  "schema": {
    "type": "function",
    "function": {
      "name": "recall",
      "description": "Hybrid search over Tomo's knowledge base (FTS lexical + semantic embeddings when available). Returns the top matching snippets.",
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "Keywords or question to search for in the knowledge base."
          },
          "limit": {
            "type": "integer",
            "description": "Maximum number of snippets to return (default 5, max 20)."
          }
        },
        "required": ["query"]
      }
    }
  }
}
