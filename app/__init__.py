"""Tomo — 友達 (tomodachi), a general-purpose agent swarm.

Web UI package. The agent backend is intentionally stubbed here; the real
coordinator runtime is implemented separately and wired in later.
"""

__version__ = "0.1.0"
